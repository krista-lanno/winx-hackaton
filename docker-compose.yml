services:
  db:
    image: postgres:15-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=winx
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
  backend:
    build: ./backend
    ports:
      - "8080:8080"